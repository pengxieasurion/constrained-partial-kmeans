// thread example
#include <iostream>       // std::cout
#include <random>         // std::thread

float* cpkmeans(float* data, long int width, long int nobs) 
{
  static float t[10][10];
  return *t;
}
